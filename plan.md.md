# バブルで「好き」を表現するWebアプリ 要件定義（案 v0.1）

作成日: 2026-09-09

## 1. 目的

自分の好きなものを、大きさの違う泡として並べて見せる。娯楽用途。分析でも プレゼンでもないので、正確さより「作るのが楽しい・見せて楽しい」を優先する。

既存サービス調査の結論として、以下の組み合わせを満たすものが見つからなかった ため自作する。

-   登録不要・数十秒で作れるカジュアルさ（みんなのTier表 相当）
-   軸なし、サイズだけで好き度を表現
-   バブルの中に画像を入れられる
-   URLを共有すれば相手がブラウザ上でそのまま見られる
-   アニメーションで見て楽しい

近いが外れたもの: pop n' plot（2軸前提・画像不可の可能性）、Flourish（データ 可視化ツールで手数が多い・英語UI・無料枠は公開必須）、日本語Tier表メーカー各種 （PNG保存が前提でインタラクティブ共有ではない）。

## 2. コンセプト

-   「Tier表のバブル版」。ランク分けの代わりに、円の面積で好き度を示す。
-   泡はゆっくり漂い、互いにぶつかって押し合う。触ると反応する。
-   閲覧者は何も入力しない。眺めて、ドラッグして遊べればよい。

## 3. スコープ

### やること

-   1画面のエディタ（アイテム追加・編集・削除）
-   泡の描画とアニメーション
-   閲覧専用URLの発行

### やらないこと（v1では）

-   アカウント、ログイン
-   共同編集、コメント、いいね
-   複数チャートの管理画面、フォルダ
-   2軸配置、カテゴリ分け、集計
-   スマホでの作成（閲覧のみ対応）

## 4. ユーザーストーリー

1.  作成者として、好きなものを名前と好き度で20個くらい登録し、すぐ泡にしたい。
2.  作成者として、泡に画像を入れて一目で何か分かるようにしたい。
3.  作成者として、できたURLをX/Slack/LINEに貼って見せたい。
4.  閲覧者として、リンクを開くだけで（登録なしに）泡が動くのを見たい。
5.  閲覧者として、泡をドラッグして押し合いを楽しみたい。
6.  閲覧者として、気に入ったら「これを元に自分でも作る」ボタンで複製したい。

## 5. 機能要件

優先度は MoSCoW（M=必須, S=あるべき, C=あればよい）。

### 5.1 アイテム管理

| ID  | 要件  | 優先度 |
| --- | --- | --- |
| F-01 | 名前（1〜20文字程度）を入力してアイテムを追加できる | M   |
| F-02 | 好き度を 1〜5 の段階または 1〜100 のスライダーで指定できる | M   |
| F-03 | アイテムを編集・削除できる | M   |
| F-04 | 改行区切りのテキストを貼り付けて一括追加できる | S   |
| F-05 | アイテム数の上限は 50 とし、超過時はエラー表示 | S   |
| F-06 | 泡の色を選べる（プリセット数色から） | C   |

### 5.2 画像

| ID  | 要件  | 優先度 |
| --- | --- | --- |
| F-10 | 画像URLを指定して泡の中に表示できる | M   |
| F-11 | ローカル画像をアップロードして表示できる | S   |
| F-12 | 画像は円形にクリップされ、はみ出しは切り取られる | M   |
| F-13 | 画像なしの場合は名前テキストのみを泡の中に表示する | M   |
| F-14 | 対応形式は JPEG / PNG / GIF / WebP、1枚あたり 5MB まで | S   |

### 5.3 表示・アニメーション

| ID  | 要件  | 優先度 |
| --- | --- | --- |
| F-20 | 軸を描かない。位置に意味はなく、大きさだけが好き度を表す | M   |
| F-21 | 泡の面積が好き度に比例する（半径ではなく面積で比例させる） | M   |
| F-22 | 泡は常時ゆっくり漂い、衝突して押し合う | M   |
| F-23 | 泡をドラッグで掴んで投げられる。離すと再び漂う | M   |
| F-24 | 追加時に泡がポップして現れる | S   |
| F-25 | 泡をタップ/クリックすると名前と好き度をポップアップ表示 | S   |
| F-26 | 最小サイズでも名前が読める（読めない場合はホバー/タップで補う） | M   |

### 5.4 保存・共有

| ID  | 要件  | 優先度 |
| --- | --- | --- |
| F-30 | 編集内容はブラウザに自動保存され、閉じても復元できる | M   |
| F-31 | 「共有」で閲覧専用URLを発行できる | M   |
| F-32 | 閲覧者はアカウント登録なしで開ける | M   |
| F-33 | 共有URLの内容は読み取り専用（閲覧者は元データを変更できない） | M   |
| F-34 | 閲覧画面から「複製して自分で編集」ができる | C   |
| F-35 | PNGとして書き出せる（静止画） | C   |

## 6. 非機能要件

-   登録・インストール不要。URLを開けば使える。
-   作成はPC想定、閲覧はスマホ必須対応。
-   泡50個で動きがカクつかないこと（目標60fps、最低30fps）。
-   初回表示は3秒以内。
-   共有URLを知る人は誰でも閲覧できる前提とし、その旨を共有時に明示する。
-   機微な情報を入れる用途ではないことをUIで示す。

## 7. データモデル（案）

```json
{
  "version": 1,
  "title": "好きなもの",
  "items": [
    { "id": "a1", "name": "紅茶", "score": 90, "image": "https://...", "color": "teal" },
    { "id": "a2", "name": "Terraform", "score": 55 }
  ]
}
```

-   `score` は 1〜100 の整数。表示半径は `r = k * sqrt(score)` で算出する。
-   `image` は省略可。省略時はテキスト表示。

## 8. 共有方式の選択肢

決めるべき最大の分岐。v1でどちらを採るかで構成が変わる。

### 案A: URLに状態を全部詰める（バックエンドなし）

-   データJSONを圧縮してURLのハッシュに載せ、静的ホスティングだけで完結する。
-   長所: サーバーもDBも不要。運用コストゼロ。データを預からない。
-   短所: 画像をURLに含められないため、画像は外部URL参照のみ（F-11不可）。 アイテムが多いとURLが長くなる。

### 案B: 保存先を1つ持つ（軽量バックエンド）

-   保存時にIDを発行し、`/v/{id}` で閲覧する。画像はオブジェクトストレージへ。
-   長所: 画像アップロード（F-11）が可能。URLが短い。
-   短所: ストレージ費用、削除・保持期間・不適切画像の扱いを決める必要がある。

判断の目安: 画像アップロードを諦められるなら案A。画像を手軽に入れられること がこの企画の肝なら案B。

## 9. 参考実装

-   MemeStats（D3 + d3-force + d3-drag）: 泡のサイズを値に対応させ、ドラッグで 互いに反発する挙動。画像はSVGの `pattern` に `image` を入れ、円の `fill` に `url(#id)` を指定して円形に貼る手法。
-   OpenTierBoy（AGPL-3.0）: アカウント不要で作成・共有できるTier表ツール。 共有まわりの体験設計の参考。

## 10. 未決事項

1.  共有方式（案A / 案B）
2.  好き度の入力UI（5段階のボタンか、連続値スライダーか）
3.  画像アップロードを入れるか（案Bとセット）
4.  泡の色をユーザーに選ばせるか、好き度から自動で決めるか
5.  公開データの削除手段（案Bの場合、編集キーを持たせるか）

## 11. 受け入れ基準（v1完成の定義）

-   何も知らない人が、初見で3分以内に10個のアイテムを登録して泡を作れる。
-   共有URLをスマホで開いて、泡が漂い、指で動かせる。
-   画像を入れた泡が円形に切り抜かれて表示される。
-   ページを閉じて開き直しても編集内容が残っている。